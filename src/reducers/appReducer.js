// ====================================================
// Reducers / Application
// ====================================================

// Constants
import { APP_ACTION_TYPES } from "../actions/actions";

const initalState = {
  isReady: false
};

export default (state = initalState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
