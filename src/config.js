// =====================================================
// src / _helpers / config.js
// =====================================================

const config = require("../config/default.json");

module.exports = config;
