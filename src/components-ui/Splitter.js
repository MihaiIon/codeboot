import React, { Component } from "react";
import PropTypes from "prop-types";

class Splitter extends Component {
  render() {
    return <div className="c-splitter" />;
  }
}

Splitter.propTypes = {};

export default Splitter;
