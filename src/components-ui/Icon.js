import React, { Fragment } from "react";
import PropTypes from "prop-types";

// // TODO: Remove svg-sprite-loader dependency to optimize production build
// const files = require.context("!svg-sprite-loader!../images/icons", false, /.*\.svg$/);
// files.keys().forEach(files);

// const Icon = ({ type, className }) => (
//   <svg className={`o-icon ${className}`} width="20px" height="20px">
//     <use xlinkHref={`#${type}`} />
//   </svg>
// );

// Icon.defaultProps = {
//   className: ""
// };
// Icon.propTypes = {
//   type: PropTypes.string.isRequired,
//   className: PropTypes.string
// };

export default Fragment;
